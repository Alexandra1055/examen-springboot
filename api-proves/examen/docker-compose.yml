services:
  mysql:
    image: mysql:8.4
    container_name: examen2526-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: sports
    ports:
      - "3306:3306"
    volumes:
      - mysql_sports_data:/var/lib/mysql

volumes:
  mysql_sports_data: